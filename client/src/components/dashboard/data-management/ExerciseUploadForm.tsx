"use client";

import React from "react";

/**
 * @task Frontend Dev 5: Exercise Upload Form
 * - Build a form to collect `IExercise` data (Name, Muscle, Difficulty, etc.)
 * - Implement dynamic text chips for `tips`.
 * - Integrate with `imageUploadService.ts` for the `image` field via ImgBB.
 * - Call `POST /api/exercises` on submit.
 * - Show Toast notifications for success/error.
 */
export default function ExerciseUploadForm() {
  return (
    <div>
      <h2 className="text-xl font-semibold mb-4">Upload New Exercise</h2>
      {/* DEV 5: Build form here */}
    </div>
  );
}
