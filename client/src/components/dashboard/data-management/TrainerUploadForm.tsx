"use client";

import React from "react";

/**
 * @task Frontend Dev 4: Trainer Upload Form
 * - Build a form to collect `ITrainer` data (Name, Bio, Experience, etc.)
 * - Integrate with `imageUploadService.ts` for the `photo` field via ImgBB.
 * - Call `POST /api/trainers` on submit.
 * - Show Toast notifications for success/error.
 */
export default function TrainerUploadForm() {
  return (
    <div>
      <h2 className="text-xl font-semibold mb-4">Upload New Trainer</h2>
      {/* DEV 4: Build form here */}
    </div>
  );
}
